# MedBook Search AI - Backend Requirements

# Core FastAPI
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.25
aiosqlite>=0.19.0

# Configuration & Settings
pydantic>=2.5.3
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# HTTP & Web
httpx>=0.26.0
aiohttp>=3.9.1

# Web Scraping
beautifulsoup4>=4.12.2
lxml>=5.1.0
playwright>=1.41.0

# PDF Processing
pymupdf>=1.23.8
pdfplumber>=0.10.3

# OCR
pytesseract>=0.3.10
Pillow>=10.2.0

# Vector Database
# NOTE: ChromaDB currently has transitive deps (onnxruntime / pulsar-client) that
# do not provide wheels for Python 3.14 on Windows. We skip installation on
# Python 3.14 and enable it again once supported (Phase 6).
chromadb>=0.4.22; python_version < '3.14'

# AI & ML
# NOTE: Similar story for large ML stacks (torch / transformers). Skip on Py3.14.
sentence-transformers>=2.2.2; python_version < '3.14'

# API Clients
anthropic>=0.14.0
deepl>=1.16.1

# Security
cryptography>=41.0.7

# Utilities
tenacity>=8.2.3
pyyaml>=6.0.1

# Testing
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-asyncio>=0.21.1

# Requests (for existing code compatibility)
requests>=2.31.0
